Question 1:    In java script var is the old way to declare variables and it can also update and re-declared where let is the modern way and cannot be re-declared but cab+n update and const  cannot be updated or re-declared.

Question 2: The spread operator is mainly  used to copy the values . For example: const 
arr1 =[1, 2];
const arr2 = [...arr1, 3, 4];
Question 3: for map it creates new array and changes every element and filter also create new element and keeps only that elements that match a condition and foreach() it just loops through items  and just return a new array.
Question 4: An arrow function is where we can write function in short way.
For example:
const add = (a, b) => a + b;
Question 5: the template literals are used to write strings with variables. For example: const name = "Khalid"; console.log(`Hello ${name}`);
